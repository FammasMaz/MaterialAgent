# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Local configuration — contains absolute SDK paths
local.properties

# IDE
.idea/
*.iml
.DS_Store

# Android
captures/
*.apk
*.aab

# Credentials — this repository is public, so nothing secret may be committed.
# `!release.keystore` used to be here, which would have re-included a signing
# key sitting in the worktree. Keystores stay out, with no exceptions.
*.keystore
*.jks
*.pem
*.p12
.env
.env.*
.hermes-test-token
auth.json
accounts/
local.properties

# Logs
*.log

# Kotlin
.kotlin/

# Brand asset pipeline intermediates (regenerated by docs/brand/build_assets.py)
docs/brand/preview/build/
